Barre de recherche de page d'accueil (démarches, destinations, propositions) ; `tone="inverse"` sur fond vert, `tone="primary"` sur fond clair.
```jsx
<SearchBar value={q} onChange={setQ} onSubmit={go} placeholder="ex. attestation de non-redevance"/>
```
