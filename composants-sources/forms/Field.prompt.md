Label + hint + error wrapper used by Input/Select; label above, hint under label, error under control.
```jsx
<Field label="Numéro CNI" hint="9 chiffres" error="Numéro invalide" required htmlFor="cni"><Input id="cni" invalid /></Field>
```