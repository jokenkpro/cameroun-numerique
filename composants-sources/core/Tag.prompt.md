Pill for categories and filters (the only rounded shape besides dots). Clickable when onClick is passed; removable with onRemove.
```jsx
<Tag>État civil</Tag> <Tag selected onClick={...}>Yaoundé</Tag> <Tag onRemove={...}>Passeport</Tag>
```