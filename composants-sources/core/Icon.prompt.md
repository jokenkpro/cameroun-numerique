Lucide icon wrapper (stroke 1.75, 20px default); use for every glyph — never emoji or hand-drawn SVG.
```jsx
<Icon name="arrow-right" /> <Icon name="file-text" size={24} label="Document" />
```
Decorative by default (aria-hidden); pass `label` when the icon carries meaning alone.